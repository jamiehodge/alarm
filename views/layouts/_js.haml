:javascript
	yepnope([{
		load: 'http://ajax.googleapis.com/ajax/libs/jquery/1.5.1/jquery.min.js',
	  complete: function () {
	    if (!window.jQuery) {
	      yepnope('/js/libs/jquery.min.js');
	    }
		}
	},{
		load: ['/js/libs/jquery.cookie.js', '/js/libs/jquery.collapse.js', '/js/libs/jquery.lazyload.min.js', '/js/script.js?v=1'],
		complete: function () {
			$('img').lazyload({
				effect : "fadeIn" 
			});
			localStorage.setItem('base_url', '#{base_url}')
		}
	}]);

/[if lt IE 7 ]
	%script{ :src => '/js/libs/dd_belatedpng.js' }
	:javascript
		//DD_belatedPNG.fix('img, .png_bg');

:javascript
	var _gaq = [['_setAccount', '#{settings.google_account_id}'], ['_trackPageview']];
	(function(d, t) {
		var g = d.createElement(t),
		s = d.getElementsByTagName(t)[0];
		g.async = true;
		g.src = ('https:' == location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
		s.parentNode.insertBefore(g, s);
	})(document, 'script');