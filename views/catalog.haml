%section.hfeed.catalog{ id: feed.id }
	%aside
		%figure
			%img{ src: feed.logo, height: '', width: '' }
	%header
		%h1.feed-title= feed.title
		.feed-subtitle= feed.subtitle
		%time.updated{ datetime: feed.updated }= feed.updated.strftime('%A, %b %d')
	%nav
		%ul
			- feed.entries.each do |entry|
				%li
					%a{ href: "/atom_feeds/#{entry.id}" }= entry.title
	.feed-content
	%footer
		
