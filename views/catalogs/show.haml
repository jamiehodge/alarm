%article#feed
	%header
		%hgroup
			%h1= catalog.title
			%p= catalog.subtitle
			%p
				%small
					%time{ :time => catalog.updated.strftime('%y-%m-%d'), :pubdate => true }= catalog.updated.strftime('%A, %b %d %Y')
		%figure		
			%img{ :src => catalog.logo, :width => '100%' }
	- catalog.entries.each do |feed|
		= haml :'entries/show', :locals => { :entry => feed }